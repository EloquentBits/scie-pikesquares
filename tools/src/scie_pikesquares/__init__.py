INSTALL_URL = "https://pikesquares.com/docs/installation"

# An arbitrary number: bump when there's a change that someone might want to query for
# (e.g. checking $(PANTS_BOOTSTRAP_TOOLS=1 ./pants version) >= ...).
VERSION = 3
